"use client";
import{useMemo,useState}from"react";import{useSearchParams}from"next/navigation";import{Shell}from"../components/Layout";import ProductCard from"../components/ProductCard";import{SearchIcon}from"../components/Icons";import{categories,products}from"../data";
export default function Shop(){const params=useSearchParams(),initial=params.get("category")||"All",[category,setCategory]=useState(categories.includes(initial)?initial:"All"),[query,setQuery]=useState("");const shown=useMemo(()=>products.filter(p=>(category==="All"||p.category===category)&&`${p.name} ${p.color}`.toLowerCase().includes(query.toLowerCase())),[category,query]);return <Shell><section className="shop-head"><p className="eyebrow">The collection</p><h1>Everyday, <em>elevated.</em></h1><p>Considered pieces designed to work together—and live beautifully on their own.</p></section><section className="shop-tools"><div className="filters">{categories.map(c=><button className={category===c?"selected":""} onClick={()=>setCategory(c)} key={c}>{c}</button>)}</div><label className="search"><SearchIcon/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search pieces"/><span>{shown.length} pieces</span></label></section><section className="section shop-grid">{shown.length?<div className="product-grid">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><h2>No pieces found</h2><p>Try another search or category.</p></div>}</section></Shell>}
