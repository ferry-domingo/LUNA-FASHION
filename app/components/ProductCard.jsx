"use client";
import Link from "next/link"; import { useState } from "react"; import { useCart } from "../CartContext"; import { money } from "../data"; import WishlistButton from "./WishlistButton"; import QuickView from "./QuickView";
export default function ProductCard({ product }) { const { add } = useCart(), [quickView, setQuickView] = useState(false); return <><article className="product-card"><div className="product-image-wrap">{product.tag && <span className="tag">{product.tag}</span>}<WishlistButton product={product} compact /><Link href={`/product/${product.id}`}><img src={product.image} alt={product.name} /></Link><div className="card-actions"><button onClick={() => setQuickView(true)}>Quick view</button><button onClick={() => add(product)} aria-label={`Quick add ${product.name}`}>Add to bag</button></div></div><Link href={`/product/${product.id}`} className="product-info"><div><h3>{product.name}</h3><p>{product.color} · {product.material || "Considered fabric"}</p></div><span>{money(product.price)}</span></Link></article>{quickView && <QuickView product={product} onClose={() => setQuickView(false)} />}</> }
